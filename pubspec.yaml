name: tang0
description: "Cross-tab sync & dedup primitives for Flutter web — presence, true leader election, CRDT conflict resolution, and state catch-up over BroadcastChannel & Web Locks."
version: 0.10.0
homepage: https://github.com/ZackaryW/tang0

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

platforms:
  web:

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.5.2
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0