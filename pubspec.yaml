name: tang0
description: "A Flutter web package for effortless cross-tab communication using BroadcastChannel API"
version: 0.9.0
homepage: https://github.com/ZackaryW/tang0

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

platforms:
  web:

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.5.2
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0